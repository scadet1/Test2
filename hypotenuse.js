const readlineSync = require ("require-sync");

const amount =(readlineSync.question("Enter base: "));
const amount2 =(readlineSync.question("Enter height: "));
const amount3 =(readlineSync.question("Enter hypotenuse: "));

const base = amount
const height = amount2
const hypotenuse = amount3

if
  const righttriangle = base ** * height ** = hypotenuse **;
  console.log("\nYes, that's a right triangle!");
}else {
  console.log("\nNope...not a right triangle.")
}
